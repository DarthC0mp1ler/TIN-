/*
Write a JS function to validate contents of a form –
the form should have at least one mandatory numerical
field and one field to be checked with a regular expression
(e.g. an e-mail). Fields not passing validation should have
their style modified to inform the user.
The JS function should return false if form fails validation,
true if it passes.
 */

function validate() {
    /*let email = document.getElementById("email").value;
    let pass = document.getElementById("pass").value;
    if(pass.length === 0){
        document.getElementById("pass").style.color = "red";
    }else{
        document.getElementById("pass").style.color = "white";
        return false;
    }
    let regex = /^[A-Za-z0-9]+@[A-Za-z0-9]+\.$[A-Za-z0-9]+/;
    if(email.test(regex)) {
        document.getElementById("email").style.color = "red";
    } else{
        document.getElementById("pass").style.color = "white";
        return false;
    }
    return true;*/
    return false;
}